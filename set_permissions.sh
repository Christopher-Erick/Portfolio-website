#!/bin/bash
# Script to set executable permissions on all build scripts

echo "Setting executable permissions on build scripts..."

# Set executable permissions on build scripts
chmod +x build
chmod +x build.sh
chmod +x build.bat

echo "Permissions set successfully!"